//
// Install artifacts locally or deploy and release articats to maven repository
//
// https://github.com/smh/grunt-maven-tasks
//
module.exports = {
	options: { 
		groupId: '<%= pkg.config.artifact.groupId %>',
	},
	debug: {
		options: {
			goal: 'deploy',
			repositoryId: '<%= pkg.config.artifact.repositories.snapshot.id %>',
			url: '<%= pkg.config.artifact.repositories.snapshot.url %>'
		},
		src: [ 'dist/**' ]
	},
	release: {
		options: {
			goal: 'release',
			repositoryId: '<%= pkg.config.artifact.repositories.release.id %>',
			url: '<%= pkg.config.artifact.repositories.release.url %>',
			commitPrefix: '<%= pkg.config.artifact.repositories.release.commit_prefix %> autogenerated by maven-task plugin: '
		},
		src: [ 'dist/**' ]
	}
}
